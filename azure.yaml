# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: langchain_poe_api_with_search
metadata:
  template: fastapi-on-azure-functions@0.0.1-beta
services:
  api:
    project: .
    language: py
    host: function
